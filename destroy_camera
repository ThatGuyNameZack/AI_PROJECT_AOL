import cv2

cam = cv2.VideoCapture(0)

if not cam.isOpened():
    print("Error: Could not open camera.")
else:
    ret, frame = cam.read()
    if not ret:
        print("Error: Could not read frame.")
    else:
        print("Camera works!")

cam.release()
